# iOS Simulator overrides — copy the relevant lines into .env when PLATFORM=ios.
# Requires macOS + Xcode. Never commit.

PLATFORM=ios
DEVICE_NAME=iPhone 16
PLATFORM_VERSION=18.0
APP_PATH=builds/cofee-dev.app
BUNDLE_ID=life.cofee.app.dev
# UDID=
